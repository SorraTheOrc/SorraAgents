# Compatibility shim package for tests expecting `skill.owner_inference`
# The real skill directory is named `owner-inference` (hyphen). This package
# exposes an importable package with the underscore name for test compatibility.
